interface:
  display_name: "Jira Find Ticket"
  short_description: "Find Jira tickets from vague prompts"
  default_prompt: "Use $jira-find-ticket to find the best Jira matches for these issue descriptions."

dependencies:
  tools:
    - type: "mcp"
      value: "jira-mcp-readonly"
      description: "Read-only Jira MCP server for issue search and details"
