interface:
  display_name: "Plan"
  short_description: "Plan development work before implementation"
  default_prompt: "Use $plan to inspect this repository and create a standalone implementation-ready Markdown plan artifact for the requested change."
