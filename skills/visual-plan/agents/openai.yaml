interface:
  display_name: "Visual Plan"
  short_description: "Create interactive HTML plan diagrams"
  default_prompt: "Use $visual-plan to map this change as an interactive HTML implementation diagram grounded in the repository."
