interface:
  display_name: "Visual Plan"
  short_description: "Map development work with a diagram"
  default_prompt: "Use $visual-plan to map this change as a diagram-first implementation plan grounded in the repository."
