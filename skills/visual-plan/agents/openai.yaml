interface:
  display_name: "Visual Plan"
  short_description: "Create clear, viewable MDX plans"
  default_prompt: "Use $visual-plan to turn this change into a clear, viewable MDX implementation plan grounded in the repository."
